# User Profile

Name: 
Timezone: Europe/London
Preferences: 
Background: 

<!-- CUSTOMISATION NOTES

This file holds basic profile information about the user. The agent reads
it on every turn to personalise responses. The agent cannot write to this
file directly; it is maintained by the user.

The agent may suggest updates (e.g. "should I note that you prefer
morning meetings?") but the user makes the edit.

Keep it brief — detailed preferences and context are better stored as
facts in the database, where they can be semantically searched and
ranked by relevance.
-->
