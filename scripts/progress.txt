# Ralph Progress Log
Milestone: skills
Started: Sun 29 Mar 2026 21:55:51 CEST
---

## Codebase Patterns
- Test fixtures use `monkeypatch.chdir(tmp_path)` to redirect file I/O; module-level `Path` constants also need patching via `monkeypatch.setattr`
- Memory subdirs are registered in `memory._SUBDIRS` — add new dirs there, not manually

## 2026-03-29 - #25 Inject matched skills into context builder
- Updated `context.build_system_prompt()` to call `skills.load_skills()` + `skills.match_skills()`
- Matched skills appended under `## Active Skills` heading (name subheading, instruction, body)
- No heading added when no skills match
- Existing truncation logic already preserves skills (non_fact bucket) and trims facts first
- Files changed: `awfulclaw/context.py`, `tests/test_context.py`
- **Learnings for future iterations:**
  - Truncation logic in context.py separates `non_fact` (preserved) from `fact` (trimmed) — skills naturally fall into non_fact so no special handling needed
  - Test isolation needs both `monkeypatch.chdir` and `monkeypatch.setattr` for module-level Path constants
---

## 2026-03-29 - #26 Teach Claude when and how to create skill files
- Added `## Skills` section to `_BASE_PROMPT` in `context.py`
- Documents skill file format (trigger/instruction frontmatter, memory:write tag, example)
- Trigger keywords: always, never, remember, should, from now on, every time, make sure
- Confirms to user with "Got it — I've saved that as a skill."
- Files changed: `awfulclaw/context.py`
- **Learnings for future iterations:**
  - Ruff E501 (100-char line limit) applies inside multiline strings too — wrap long prose lines
---

## 2026-03-29 - #24 Add skills module with SKILL.md load and match
- Created `awfulclaw/skills.py` with `Skill` dataclass, `load_skills()`, `match_skills()`
- Added `skills` to `memory._SUBDIRS` so directory is auto-created
- Manual YAML frontmatter parsing with regex (no PyYAML per spec)
- Whole-word case-insensitive trigger matching via `\b` word boundaries
- Files changed: `awfulclaw/skills.py`, `awfulclaw/memory.py`, `tests/test_skills.py`
- **Learnings for future iterations:**
  - Module-level `Path` constants (e.g. `_SKILLS_DIR`) must be patched in tests with `monkeypatch.setattr(module, "_SKILLS_DIR", tmp_path / ...)`
  - Frontmatter parser: find second `---` by searching from index 3 to avoid matching the opening delimiter
---
