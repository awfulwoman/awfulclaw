# Ralph Progress Log
Milestone: schedule-condition-2
Started: Mon 30 Mar 2026 00:53:08 CEST
---

## [2026-03-30] - #62 Add optional condition field to Schedule model
- Added `condition: str | None = None` to `Schedule` dataclass and `Schedule.create()`
- `_to_dict()` omits `condition` key when None; `_from_dict()` defaults to None if absent
- Added 3 new tests covering condition serialization round-trips
- Files changed: `awfulclaw/scheduler.py`, `tests/test_scheduler.py`
- **Learnings for future iterations:**
  - `_to_dict` / `_from_dict` are module-level functions (not methods) — update both
  - Schedule dataclass fields with defaults must come after fields without defaults
---

## [2026-03-30] - #63 Evaluate schedule condition before invoking Claude
- Added `_should_wake(condition)` helper in `loop.py` using `subprocess.run(..., shell=True, timeout=10)`
- Condition fails open on non-zero exit, timeout, or invalid JSON
- `sched.last_run` updated even when condition suppresses Claude call
- 6 new tests in `tests/test_loop_condition.py`
- Files changed: `awfulclaw/loop.py`, `tests/test_loop_condition.py`
- **Learnings for future iterations:**
  - `_should_wake` is a module-level function — can be imported directly in tests
---

## [2026-03-30] - #64 Support condition attribute in <skill:schedule> tag parsing
- Extracted `condition` attribute in `handle_match` inside `_parse_and_apply_schedule_tags`
- Passes condition to `Schedule.create()` for both cron and one-off schedules
- Updated `_DEFAULT_SOUL` in `context.py` and `memory/SOUL.md` was already updated
- 2 new tag-parsing tests added to `tests/test_loop_condition.py`
- Files changed: `awfulclaw/loop.py`, `awfulclaw/context.py`, `tests/test_loop_condition.py`
- **Learnings for future iterations:**
  - `memory/SOUL.md` on disk can diverge from `_DEFAULT_SOUL` — the live file is what Claude reads
  - ruff import sorting: alphabetical within stdlib/third-party/local groups
---
