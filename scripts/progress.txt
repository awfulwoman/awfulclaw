# Ralph Progress Log
Milestone: soul-md
Started: Sun 29 Mar 2026 22:26:37 CEST
---

## [2026-03-29] - #32 Load SOUL.md as the base system prompt
- Replaced hardcoded `_BASE_PROMPT` with `_load_soul()` that reads `memory/SOUL.md` on each call
- Added `_DEFAULT_SOUL` constant used to seed the file if absent
- Files changed: `awfulclaw/context.py`
- **Learnings for future iterations:**
  - `memory.read()` returns empty string if file doesn't exist — use this to detect missing files
  - The truncation path in `build_system_prompt` had a local reference to `_BASE_PROMPT` that also needed updating
---

## [2026-03-29] - #34 Load HEARTBEAT.md as the idle-tick prompt
- Added `_load_heartbeat()` that reads `memory/HEARTBEAT.md` on every idle tick; seeds file with `_DEFAULT_HEARTBEAT` if absent
- Removed `_IDLE_PROMPT` constant; replaced usage in idle tick with `_load_heartbeat()` call
- Files changed: `awfulclaw/loop.py`
- **Learnings for future iterations:**
  - `memory.read()` returns empty string if file doesn't exist — use this to detect missing files (same pattern as SOUL.md in #32)
  - Re-reading on every tick means edits take effect without restart — no caching needed
---
