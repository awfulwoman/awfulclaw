# Ralph Progress Log
Milestone: launchd-service
Started: Sun 29 Mar 2026 23:20:07 CEST
---

## [2026-03-29] - #57 Add launchd plist for running awfulclaw as a macOS service
- Created `launchd/ai.awfulclaw.agent.plist` with Label `ai.awfulclaw.agent`, RunAtLoad/KeepAlive true, stdout/stderr to `__PROJECT_DIR__/logs/`
- Used two placeholders: `__PROJECT_DIR__` for working directory/log paths, `__UV_PATH__` for uv binary (detected by install script)
- **Learnings for future iterations:**
  - The issue said "ProgramArguments uses `__PROJECT_DIR__` placeholder for the uv path prefix" but uv lives outside the project dir — used `__UV_PATH__` separately and the install script substitutes both
---

## [2026-03-29] - #58 Add install/uninstall shell scripts for the launchd service
- Created `scripts/install-service.sh`: detects uv via `which uv`, creates `logs/`, substitutes `__PROJECT_DIR__` and `__UV_PATH__`, writes to `~/Library/LaunchAgents/`, loads with launchctl
- Created `scripts/uninstall-service.sh`: unloads (ignores error), removes plist
- **Learnings for future iterations:**
  - `sed` with `|` delimiter avoids issues with `/` in paths
  - `launchctl unload 2>/dev/null || true` handles "not loaded" gracefully
---

## [2026-03-29] - #59 Document launchd service setup in README
- Added "Running as a Service (macOS)" section after "Running" section
- Covers install, .env prerequisite, log locations, manual stop/start, uninstall
- **Learnings for future iterations:**
  - Issue said "under 20 lines" — kept it tight at ~18 lines of content
---
