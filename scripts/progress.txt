# Ralph Progress Log
Milestone: auth-and-docs
Started: Sun 29 Mar 2026 20:12:02 CEST
---

## 2026-03-29 - #11 Switch auth from API key to Claude CLI OAuth credentials
- Added `get_auth_token()` to `config.py` reading from `~/.claude/.credentials.json` under `claudeAiOauth.accessToken`
- `ANTHROPIC_API_KEY` env var takes priority as override (for CI)
- `claude.py` uses `auth_token=` instead of `api_key=`
- Added `tests/test_config.py` with 5 tests
- Fixed pre-existing pyright issue in `test_memory.py` with `# pyright: ignore[reportPrivateUsage]`
- Added `testpaths = ["tests"]` to fix pytest collecting `pull/` worktree
- Added `exclude = ["pull", ".venv"]` to pyright config
- **Learnings for future iterations:**
  - The Claude CLI stores OAuth tokens in `~/.claude/.credentials.json` under `claudeAiOauth.accessToken` (NOT in `settings.json`)
  - The Anthropic SDK accepts `auth_token=` as an alternative to `api_key=` for OAuth tokens
  - There's a `pull/` worktree directory in this repo that conflicts with pytest collection — always set `testpaths = ["tests"]`
  - Pyright scans `.venv` by default unless explicitly excluded
  - The `CLAUDE_CODE_OAUTH_TOKEN` env var is another auth option supported by the CLI
---

## 2026-03-29 - #12 Update README with iMessage setup and usage instructions
- Rewrote README.md from a single-line stub to a full setup guide
- Sections added: Overview, Requirements, iMessage Setup, Installation, Configuration, Running, Memory
- Auth section references `claude` CLI credentials (consistent with #11)
- Files changed: README.md only
- Typecheck: 0 errors
- **Learnings for future iterations:**
  - README-only PRs require no code changes; pyright still passes
  - The iMessage Full Disk Access requirement is a key setup step users commonly miss
---
