# Ralph Progress Log
Milestone: schedule-condition
Started: Mon 30 Mar 2026 00:06:30 CEST
---

## Codebase Patterns
- Ruff E501 (line > 100) errors are enforced — wrap long lines using multi-line function calls or string concatenation
- Staged but uncommitted changes may exist from prior incomplete runs — always check `git status` and `git diff --staged` before starting
- Pre-existing E501 errors exist in test_skills.py and test_telegram.py; do not fix those unless the story requires it

---

## [2026-03-30] - #62 Add optional condition field to Schedule model
- Added `condition: str | None = None` to `Schedule` dataclass
- `to_dict()` / `from_dict()` serialization updated (omits key when None)
- Files changed: `awfulclaw/scheduler.py`
- **Learnings for future iterations:**
  - Already committed prior to this session; verified via git log

---

## [2026-03-30] - #63 Evaluate schedule condition before invoking Claude
- Condition evaluation added before Claude invocation for due schedules
- Runs shell command, parses JSON `wakeAgent` field; fails open on error
- `sched.last_run` updated even when condition suppresses the call
- Files changed: `awfulclaw/loop.py`, `tests/test_loop_condition.py`
- **Learnings for future iterations:**
  - Already committed prior to this session; verified via git log

---

## [2026-03-30] - #64 Support condition attribute in <skill:schedule> tag parsing
- Extracts `condition` attribute from `<skill:schedule>` tags
- Passes condition to `Schedule.create()` for both cron and one-off schedules
- Updated `_DEFAULT_SOUL` in context.py and `memory/SOUL.md`
- Fixed E501 lint errors across context.py, loop.py, scheduler.py introduced by this milestone
- Files changed: `awfulclaw/loop.py`, `awfulclaw/context.py`, `memory/SOUL.md`
- **Learnings for future iterations:**
  - Staged but uncommitted reverts of prior work were present at start — always restore with `git restore --staged && git restore`
  - Ruff E501 errors must be fixed before considering quality checks passed

---
