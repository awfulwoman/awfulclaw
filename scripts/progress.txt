# Ralph Progress Log
Milestone: extensibility
Started: Mon 30 Mar 2026 13:18:13 CEST
---

## [2026-03-30] - #97 Per-sender personality overlays in system prompt
- Added `_extract_personality_section()` helper with regex to extract `## Personality` section (stops at next `##` heading)
- Modified `_load_soul(channel)` to check for `SOUL_<channel>.md` before falling back to `SOUL.md`
- Restructured `build_system_prompt()` to resolve sender's person file early (before building sections list) so personality overlay can be applied to `soul` var before it's used in `sections`
- Files changed: `awfulclaw/context.py`, `tests/test_context.py`
- **Learnings for future iterations:**
  - `build_system_prompt` builds `sections` list with `soul` as `sections[1]` — any modifications to `soul` must happen BEFORE the sections list is built
  - `_find_person_by_phone()` returns `(filename, content)` tuple — already exists and can be reused
  - All existing helper functions in context.py use `_` prefix (private convention) — keep consistent
  - Pre-existing ruff errors in test_telegram.py and startup_briefing module (67 pyright errors, 2 ruff errors before this issue)
---

## [2026-03-30] - #96 Generic tool dispatch loop with pluggable matchers
- Defined `ToolMatcher` Protocol in `awfulclaw/modules/base.py` with `match()` and `execute()` methods
- Added `SkillTagMatcher` class in `base.py` — wraps `Module + SkillTag`, strips tag, dispatches, updates history, returns cleaned reply
- Added `get_all_tool_matchers()` to `ModuleRegistry` returning `list[SkillTagMatcher]`
- Refactored `_dispatch_all_skills()` → `_dispatch_tools()` in `loop.py`; loop calls `claude.chat()` after each `execute()`; cleanup loop uses `_` for unused `module` variable
- Files changed: `awfulclaw/modules/base.py`, `awfulclaw/modules/__init__.py`, `awfulclaw/loop.py`
- **Learnings for future iterations:**
  - `SkillTagMatcher.execute()` updates history in-place and returns cleaned reply; the loop is still responsible for calling `claude.chat()` after execute — keeps matchers free of Claude dependency
  - Pyright flags unused loop variables — use `_` for the unused element of a tuple unpack
  - `get_all_tool_matchers()` in registry wraps `get_all_skill_tags()` — adding new ToolMatcher types requires a new method/factory in the registry or building matchers in `loop.py`
---

## [2026-03-30] - #95 Gateway abstraction: decouple message ingestion from agent loop
- Created `awfulclaw/gateway.py` with `Gateway` class: owns `dict[str, Connector]`, `queue.Queue[Message]` inbound queue, `threading.Event` stop signal, and background polling threads per connector
- Added `channel: str = "telegram"` to `Message` dataclass (default preserves backward compat)
- Added `get_channel()` to `config.py` so `__main__.py` can name the channel when building the gateway
- Changed `loop.run(connector)` → `loop.run(gateway)`: drains `gateway.get_messages()` each tick; sends via `gateway.send(channel, recipient, body)`; calls `gateway.start()` on entry and `gateway.stop()` on exit
- Updated `__main__.py` to create `Gateway([(channel, connector)])` before calling `loop.run()`
- Files changed: `awfulclaw/gateway.py` (new), `awfulclaw/connector.py`, `awfulclaw/config.py`, `awfulclaw/loop.py`, `awfulclaw/__main__.py`
- **Learnings for future iterations:**
  - `queue.Queue` (threading) is the right choice here — loop is synchronous, asyncio would require a full rewrite
  - Gateway background threads use `threading.Event.wait(timeout)` for the poll interval — avoids busy-loop and responds to `stop()` promptly
  - `primary_recipient_for(channel)` method lets message-handler replies route back to the correct channel without exposing `_connectors` dict
  - Pre-existing pyright errors (67) and ruff errors (2) in test_telegram.py and startup_briefing — don't touch those files
  - The `now = datetime.now(timezone.utc)` line in the idle block was unused after the gateway removed `last_poll` tracking — removed it to avoid pyright warnings
---

## [2026-03-30] - #102 Add mcp dependency and create MCP config generator
- Added `mcp>=1.26.0` to pyproject.toml via `uv add mcp`
- Created `awfulclaw/mcp/__init__.py` with `generate_mcp_config()` — uses `mkstemp` (not deprecated `mktemp`) to write `{"mcpServers": ...}` JSON to a temp file
- Created `awfulclaw/mcp/registry.py` with `MCPRegistry`: `register(name, command, args, env)` + `generate_config() -> Path` + `is_empty()`
- Files changed: `awfulclaw/mcp/__init__.py` (new), `awfulclaw/mcp/registry.py` (new), `tests/test_mcp_registry.py` (new), `pyproject.toml`, `uv.lock`
- **Learnings for future iterations:**
  - Use `tempfile.mkstemp()` not `mktemp()` — pyright flags mktemp as deprecated
  - `uv add mcp` auto-updates both `pyproject.toml` and `uv.lock`
  - Pre-existing baseline: 104 pyright errors (test_telegram.py), 1 ruff error (test_telegram.py line length) — don't touch those files
  - The MCP config format the claude CLI expects: `{"mcpServers": {"name": {"command": ..., "args": [...], "env": {...}}}}`
---

## [2026-03-30] - #104 Create MCP server for memory_write
- Created `awfulclaw/mcp/memory_write.py`: FastMCP stdio server with `memory_write(path, content)` tool
- Routes `facts/` and `people/` to SQLite; all other paths go to `memory.write()`
- Blocks `SOUL.md`, `HEARTBEAT.md`, `skills/`; path traversal caught by `memory._resolve()`
- Registered in `MCPRegistry` at startup in `loop.py`
- Files changed: `awfulclaw/mcp/memory_write.py` (new), `tests/test_mcp_memory_write.py` (new), `awfulclaw/loop.py`
- **Learnings for future iterations:**
  - Use typed pytest fixtures (`tmp_path: Path`, `monkeypatch: pytest.MonkeyPatch`) in an `autouse` fixture to get 0 pyright errors — untyped inline fixtures cause pyright warnings
  - FastMCP tool functions are plain functions decorated with `@mcp.tool()`, run via `mcp.run()` as `__main__`
  - `memory._resolve()` already guards path traversal with `ValueError` — catch and return error string
---

## [2026-03-30] - #103 Wire --mcp-config into claude.py
- Added `mcp_config_path: Path | None` parameter to `chat()` and `_chat_cli()` in `awfulclaw/claude.py`
- When provided, `--mcp-config <path>` appended to the subprocess command
- `loop.py` creates `MCPRegistry()` at startup; generates config path (None when empty); passes through `_chat_async` closure and all `_dispatch_tools` call sites
- Created `tests/test_claude.py` with 3 tests
- Files changed: `awfulclaw/claude.py`, `awfulclaw/loop.py`, `tests/test_claude.py` (new)
- **Learnings for future iterations:**
  - `_chat_async` is a closure inside `run()` — it captures `mcp_config_path` from the outer scope, so no signature change needed; just use it in the lambda
  - `_dispatch_tools` is a module-level async function so it does need the parameter threaded through explicitly
  - All 104 pre-existing pyright errors are in test_telegram.py and test_mcp_registry.py — baseline remains unchanged
---

## [2026-03-30] - #105 Create MCP server for web search
- Created `awfulclaw/mcp/web.py`: FastMCP stdio server with `web_search` tool
- Imports `search()` from `awfulclaw/modules/web/_web.py` — no logic duplication
- Registered in `MCPRegistry` at startup in `loop.py` (alongside `memory_write`)
- Old web module untouched
- 6 unit tests in `tests/test_mcp_web.py` with mocked search calls
- Files changed: `awfulclaw/mcp/web.py` (new), `tests/test_mcp_web.py` (new), `awfulclaw/loop.py`
- **Learnings for future iterations:**
  - MCP server pattern: import logic from existing module, thin wrapper with `@mcp.tool()`, `mcp.run()` in `__main__`
  - Register in `loop.py` `MCPRegistry` with `["run", "python", "-m", "awfulclaw.mcp.<name>"]` args
  - Test pattern: mock the imported function (e.g. `awfulclaw.mcp.web.search`), call the tool handler directly
  - Pre-existing baseline: 104 pyright errors in test_telegram.py — unchanged
---

## [2026-03-30] - #106 Create MCP server for memory search
- Created `awfulclaw/mcp/search.py`: FastMCP stdio server with `memory_search` tool
- Reuses `search_facts`, `search_people`, `memory.search_all`, and `get_db` directly — no logic duplication
- Registered in `MCPRegistry` at startup in `loop.py`
- Old search module untouched and still functional
- Files changed: `awfulclaw/mcp/search.py` (new), `tests/test_mcp_search.py` (new), `awfulclaw/loop.py`
- **Learnings for future iterations:**
  - When a module's search logic is in a `dispatch()` method, import the underlying DB/memory functions directly instead of trying to call dispatch with a mock match object
  - Test fixture `autouse=True` with `monkeypatch` lets you set safe defaults; individual tests override only what they need
  - `lambda: (_ for _ in ()).throw(...)` is not valid syntax for raising in a lambda — use a named `_raise()` function instead
---

## [2026-03-30] - #107 Create MCP server for schedule
- Created `awfulclaw/mcp/schedule.py`: FastMCP stdio server with `schedule_create`, `schedule_delete`, `schedule_list` tools
- Reuses `awfulclaw/scheduler.py` directly (load_schedules, save_schedules, Schedule.create) — no logic duplication
- Registered in `MCPRegistry` at startup in `loop.py`
- Old `schedule` module untouched and still functional
- 12 unit tests in `tests/test_mcp_schedule.py` with mocked scheduler calls
- **Learnings for future iterations:**
  - `list[str]` annotation needed on empty list vars used as `lines` to avoid pyright "partially unknown" errors
  - Schedule MCP pattern: thin wrapper calling scheduler module directly; no re-implementation needed
  - `autouse=True` fixture with monkeypatch patches `load_schedules` to return [] and `save_schedules` to no-op — individual tests override with `patch()` context manager when they need to inspect saved data
---

## [2026-03-30] - #109 Remove skill tag documentation from system prompt
- Removed `## Available Skills` section (built from `registry.get_system_prompt_fragments()`) from `build_system_prompt()`
- Removed `<memory:write>` tag syntax docs from `memory_instructions` block; retained profile update guidance and memory summary instructions
- Removed `get_registry` import from `context.py` — no longer referenced
- Updated `tests/test_context.py`: deleted 3 tests for removed feature, added 2 asserting absence of old sections
- Files changed: `awfulclaw/context.py`, `tests/test_context.py`
- **Learnings for future iterations:**
  - `memory_instructions` string in `context.py` is a multi-paragraph block — edit carefully to preserve only the non-skill paragraphs
  - After removing a feature, check for now-unused imports (e.g. `from unittest.mock import patch`) — ruff flags them
---

## [2026-03-30] - #108 Create MCP server for imap
- Created `awfulclaw/mcp/imap.py`: FastMCP stdio server with `imap_read` tool
- Reuses `fetch_unread()` from `awfulclaw/modules/imap/_imap.py` — no logic duplication
- Registered in `MCPRegistry` in `loop.py` only when `IMAP_HOST`, `IMAP_USER`, `IMAP_PASSWORD` env vars are all set; passes all 4 IMAP vars in the `env` block for the subprocess
- Old `imap` module untouched and still functional
- 6 unit tests in `tests/test_mcp_imap.py` with mocked `fetch_unread` calls
- Files changed: `awfulclaw/mcp/imap.py` (new), `tests/test_mcp_imap.py` (new), `awfulclaw/loop.py`
- **Learnings for future iterations:**
  - IMAP MCP pattern: thin wrapper around `fetch_unread()`, conditional registration based on env var presence
  - Pass env vars explicitly in the `env` dict when registering conditional MCP servers — subprocess won't inherit them otherwise
  - Test with `patch("awfulclaw.mcp.imap.fetch_unread", ...)` — mock the import in the MCP module, not the source module
---

## [2026-03-30] - #110 Remove regex dispatch from loop.py
- Removed `_MEMORY_WRITE_RE`, `_parse_and_apply_memory_writes()`, `_dispatch_tools()`, `_PROTECTED_FILES/DIRS`, `_is_protected_path()` from `loop.py`
- Removed `write_person` import (no longer needed after dispatch removal)
- Removed all call sites across `_handle_messages`, `_run_idle_tick`, and startup briefing block
- Reply from `claude.chat()` is now sent directly to the connector
- Hook modules (briefing, startup_briefing) and hot-reload via `registry.check_for_changes()` remain untouched
- Files changed: `awfulclaw/loop.py`
- **Learnings for future iterations:**
  - `memory` module was still needed in loop.py for `_load_heartbeat()` — don't remove it when cleaning up `_parse_and_apply_memory_writes`
  - `ev_loop` is still used by `_chat_async` (run_in_executor) even after dispatch removal — keep it
  - Baseline pyright error count for loop.py was 34 (pre-existing) — verify count stays same after changes
---
